server:
  port: 80
spring:
  application:
    name: cloud-consumer-order80

eureka:
  client:
    #表示将自己注册到注册中心
    register-with-eureka: true
    #表示去服务中心检索服务
    fetchRegistry: true
    service-url:
      defaultZone: http://localhost:7001/eureka